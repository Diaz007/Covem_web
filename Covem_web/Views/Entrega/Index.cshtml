@model IEnumerable<Covem_web.Automoviles>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout_Principal.cshtml";
}

<div class="card">
    <div class="card-body">
        <h4 class="card-title">Lista de ordenes de servicio</h4>
        <p>
            @Html.ActionLink("Registrar Nuevo Modelo", "Create")
        </p>

        <div class="row">
            <div class="col-12">
                <div class="table-responsive">
                    <table id="order-listing" class="table">
                        <thead>
                            <tr>
                                <th>Auto</th>
                                <th>Taller </th>
                                <th>Estatus </th>
                                <th>Acciones</th>

                            </tr>
                        </thead>
                        <tbody>

                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td class="py-1">
                                        <div class="d-flex align-items-center">
                                            <img src="@item.marca.foto" alt="image" />
                                            <div class="ms-3">
                                                <p class="mb-0">@item.marca.Marca1  @item.modelo.modelo1</p>
                                            </div>
                                        </div>

                                    </td>

                                    @*<td>@item.Taller.nombreTaller (@item.Taller.ubicacion) </td>*@
                                    <td>@item.EntregaRecepcion.estatus</td>
                                    <td>
                                        @Html.ActionLink("Editar", "edit", new { id = item.EntregaRecepcion.id }) |
                                        @Html.ActionLink("Terminar", "Terminar", new { id = item.EntregaRecepcion.id }) |
                                        @* @Html.ActionLink("Eliminar", "Delete", new { id = item.EntregaRecepcion.id, @class = "btn btn-outline-success" }, new { onclick = "return confirm('Seguro de eliminar esta Marca? ');" })*@

                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
